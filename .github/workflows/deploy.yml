name: Deploy CRM to Hostinger

on:
  push:
    branches:
      - main
    paths:
      - 'crm/**' # გაეშვება მხოლოდ მაშინ, თუ crm ფოლდერში რამე შეიცვლება
  workflow_dispatch: # 👈 ეს ხაზი ღილაკს ტოვებს

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        
        # 👇 მთავარი ცვლილება:
        local-dir: ./crm/      # იღებს ფაილებს მხოლოდ CRM ფოლდერიდან
        server-dir: ./portal/  # ყრის ჰოსტინგზე portal ფოლდერში
        
        exclude: |
          **/.git*
          **/.github*
          README.md
          .env

