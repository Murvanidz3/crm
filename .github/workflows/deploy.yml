name: Deploy CRM to Hostinger

on:
  push:
    branches:
      - main
    paths:
      - 'crm/**' # გაეშვება მხოლოდ მაშინ, თუ crm ფოლდერში რამე შეიცვლება
  workflow_dispatch: # 👈 ეს ხაზი ღილაკს ტოვებს

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    # Deploy public folder contents to portal root (subdomain document root)
    - name: 📂 Deploy public files to root
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./crm/public/     # public ფოლდერის შიგთავსი
        server-dir: ./portal/       # portal root-ში (subdomain document root)
        dangerous-clean-slate: false
        exclude: |
          **/.git*
          **/.github*

    # Deploy other Laravel files to portal folder (excluding public)
    - name: 📂 Deploy Laravel core files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./crm/            # CRM ფოლდერის ყველა ფაილი
        server-dir: ./portal/       # portal ფოლდერში
        dangerous-clean-slate: false
        exclude: |
          **/.git*
          **/.github*
          **/public/**
          README.md
          .env
          *.md

