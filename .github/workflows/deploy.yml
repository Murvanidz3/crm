name: Deploy CRM to Hostinger

on:
  push:
    branches:
      - main
    paths:
      - 'crm/**' # გაეშვება მხოლოდ მაშინ, თუ crm ფოლდერში რამე შეიცვლება
  workflow_dispatch: # 👈 ეს ხაზი ღილაკს ტოვებს

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    # Deploy all CRM files to portal folder
    - name: 📂 Deploy CRM files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./crm/            # CRM ფოლდერის ყველა ფაილი
        server-dir: ./domains/onecar.ge/public_html/portal/  # სრული path portal ფოლდერში
        dangerous-clean-slate: false # არ წაშალოს არსებული ფაილები
        exclude: |
          **/.git*
          **/.github*
          README.md
          .env
          *.md

